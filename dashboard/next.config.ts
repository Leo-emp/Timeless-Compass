import type { NextConfig } from "next";

// # Production-ready config — API routes are built into Next.js now
// # No external Python server needed
const nextConfig: NextConfig = {};

export default nextConfig;
